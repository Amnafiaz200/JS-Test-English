"use strict"; //strict mode, treat all js code as newer version

//alert("Hello World"); //popup message, we are using nodejs so it will give error, not using browser

//BigInt used for large numbers
//null , is a standalone value, used for empty
//undefined, means a variable is declared but not defined
//symbol , in react when use lots of components, for uniqueness.

console.log(typeof "Hi"); 
console.log(typeof 123);
console.log(typeof 123n); 
console.log(typeof true);
console.log(typeof null); 
console.log(typeof undefined); 
console.log(typeof Symbol("id"));
// null data type is object, undefined datatype is undefined


