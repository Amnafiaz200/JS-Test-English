// control flow or logic flow
// code should be run on conditional basis
// if (condition true or false){scope}

// = operator assignment, == is comparison operator, === check value and datatype.
// if (2!=4){
//     console.log('amna'); // if condition is true=> print console
    
// }
let temp = 50
// if (temp <= 49){
//     const fewer = 'high'
//     console.log(`the fewer is ${fewer}`);
// }
// else
//     {console.log('the fewer is low')};

// if (temp <= 49){
//     const fewer = 'high'
//     console.log(`the fewer is ${fewer}`);
// }
// else
//    {console.log(`the fewer is ${fewer}`)}; // // error bz fewer variable is out of scope of {}
// if use var(global scoped), the above can be assessed outside the if scope

// implicit scope
// if (temp >= 49) console.log('high'); 

// check multiple conditions:
let debitcard = true
let creditcard = true

if (debitcard && creditcard) console.log('u can buy'); //check both conditions
//  || it is or symbol.












